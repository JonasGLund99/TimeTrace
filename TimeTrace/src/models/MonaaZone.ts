/**
   * Is a class that contains the matches found in the log file.
   * The matches are represented as an array of numbers, corresponding to the
   * file lines that represent the event that was a part of the match.  
   */
export class MonaaZone {
      /**
   * @description  
   * The title of the book.
   */
    public match: number[] = []
}